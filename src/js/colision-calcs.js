let pt = Number(player.style.top.replace('px', ''))
let ft = Number(food.style.top.replace('px', ''))

let pl = Number(player.style.left.replace('px', ''))
let fl = Number(food.style.left.replace('px', ''))


